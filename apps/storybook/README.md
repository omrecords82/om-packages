# Storybook

Local preview workspace for Orthodox Metrics packages.

The generated-token preview imports `@om/tokens/css` and uses approved `data-om-*` attributes to demonstrate Phase 1B artifacts. It is not a production component library, does not define final visual design values, and must not be deployed directly from this repository.
