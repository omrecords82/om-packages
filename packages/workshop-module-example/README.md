# `@om/workshop-module-example`

Minimal OM Workshop module used to prove contracts, SDK, and module UI wiring.

Exports:

- `exampleModuleManifest` — validated `WorkshopModuleManifest`
- `ExampleModulePage` — page that loads host status/capabilities through `@om/workshop-sdk`
