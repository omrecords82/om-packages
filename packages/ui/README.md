# @om/ui

Bootstrap-only Orthodox Metrics UI integration package.

This package does not contain production UI primitives yet. The existing placeholder remains unstable and private.

Do not add real UI primitives in Phase 1A. The package may only be adjusted enough to compile against revised contracts and token-source metadata.
