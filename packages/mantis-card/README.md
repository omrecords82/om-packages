# @om/mantis-card

`MainCard` and `SubCard` from Mantis, sanitized for library use:

- Highlighter / code-highlight demo path removed from MainCard
- SubCard uses `ThemeMode` from `@om/mantis-shared/config`
